apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-db-credentials
  namespace: openwebui
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-homelab
    kind: ClusterSecretStore
  target:
    name: openwebui-db-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
        connection: "postgresql+asyncpg://{{ .username }}:{{ .password }}@postgres-cluster-rw.postgres-cluster.svc:5432/openwebui"
  data:
    - secretKey: username
      remoteRef:
        key: postgres-cnpg/openwebui
        property: username
    - secretKey: password
      remoteRef:
        key: postgres-cnpg/openwebui
        property: password
