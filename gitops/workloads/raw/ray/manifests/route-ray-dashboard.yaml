apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ray-dashboard
  namespace: ray
spec:
  parentRefs:
    - name: default-gateway
      namespace: gateway-system
  hostnames:
    - "ray-dashboard.homelab.xiehang.com"
  rules:
    - backendRefs:
        - name: ray-vllm-head-svc
          namespace: ray
          port: 8265
