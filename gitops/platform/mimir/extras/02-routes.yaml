apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mimir
  namespace: mimir
spec:
  parentRefs:
    - name: default-gateway
      namespace: gateway-system
  hostnames:
    - "mimir.homelab.xiehang.com"
  rules:
    - backendRefs:
        - name: mimir-nginx
          port: 80
